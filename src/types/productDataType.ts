export interface ProductIdListByCategoryType {
    productId: number,
}

export interface ProductCardDataType {
    productBrand: string,
    productName: string,
    productPrice: number,
    discountRate: number,
    productStar: number,
    reviewCount: number,
}

export interface ProductThumbnailDataType {
    productImageDescription: string,
    productImageUrl: string,
}